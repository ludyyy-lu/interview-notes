# 消息队列（Kafka / RabbitMQ）高频面试题 · 对话版

> 目标：把“概念解释”讲到“线上可落地”：可靠性语义、顺序、积压治理、幂等与事务。

---

## Kafka

### 1）topic / partition / offset 是什么？有什么用？

**面试官：** Kafka 的 topic、partition、offset 分别是什么？

**候选人：**
- **Topic**：逻辑上的消息类别（同一类业务事件的集合）。
- **Partition**：topic 的物理分片。每个分区是一条追加写的有序日志。
- **Offset**：消息在“某个分区内”的位置标识（单调递增）。

**面试官：** 分区带来什么好处？

**候选人：** 两个核心收益：
- **吞吐扩展**：生产/消费都能并行到多个分区。
- **顺序保证边界**：Kafka 的顺序保证通常是“**分区内有序**”。

**面试官：** 消费者组（consumer group）是什么？

**候选人：** 同一组内多个消费者共同消费一个 topic：
- 一个分区同一时刻只会分配给组内一个消费者实例（避免重复消费）。
- 组内扩容能提升并行度，但上限是分区数。

---

### 2）生产者/消费者工作流程（含关键参数）

**面试官：** 生产者写入 Kafka 的流程你怎么讲？

**候选人：** 生产者核心流程：
1. 选择分区：按 key 哈希（保证同 key 顺序）或自定义 partitioner。
2. 批量与压缩：在客户端 batch（提升吞吐），可用 gzip/snappy/lz4/zstd 压缩。
3. 发送到 leader：分区有 leader 与 follower 副本。
4. 确认机制：由 `acks` 控制确认强度。

**面试官：** `acks`、`retries`、幂等分别影响什么？

**候选人：**
- `acks=0/1/all`：
	- 0：不等确认，吞吐高但丢消息风险大
	- 1：leader 写入即确认，leader 掉电仍可能丢
	- all：等待 ISR 副本确认（配合 `min.insync.replicas`），可靠性更高
- `retries`：网络抖动/暂时错误时重试；但**重试会带来重复**风险。
- **幂等生产者**（enable idempotence）：通过 producerId + 序列号避免“重试导致重复写入”。

**面试官：** 消费者流程？

**候选人：** 消费者是 pull 模式：
1. `poll()` 拉取数据（fetch）
2. 业务处理
3. 提交 offset（自动或手动）

关键点：
- **提交 offset 的时机**决定了语义（至多一次/至少一次）。
- 需要关注 rebalance（再均衡）对停顿的影响。

---

### 3）消息投递语义：至少一次/至多一次/恰好一次

**面试官：** Kafka 能做到 exactly-once 吗？

**候选人：** 先分清语义：
- **At most once（至多一次）**：先提交 offset 再处理，可能丢。
- **At least once（至少一次）**：先处理再提交 offset，可能重复。
- **Exactly once（恰好一次）**：需要“写入幂等 + 处理与提交原子化”。

Kafka 生态里，exactly-once 通常指 Kafka 内部链路（Kafka → Kafka）：
- 幂等生产者 + 事务（transactional producer）
- 消费-处理-产出在同一事务里，并在事务中提交 offset（read_committed）

**面试官：** 如果消费端要写 MySQL，怎么保证“不重不丢”？

**候选人：** 工程上常用两类：
1. **幂等消费**：以业务唯一键去重（如订单号/事件 id），DB 用唯一索引/状态机保证重复写无害。
2. **Outbox/Inbox 模式**：
	 - Outbox：业务写 DB 时同时写 outbox 表，再由后台把 outbox 投递到 MQ。
	 - Inbox：消费落库前先写“已处理事件表”做去重。

这类比“分布式事务硬扛强一致”更稳、更可运维。

---

### 4）怎么解决消息积压（consumer lag）？

**面试官：** 线上出现消息积压你怎么处理？不要只说“加机器”。

**候选人：** 我会按“定位瓶颈 → 快速止血 → 根因治理”来做。

#### 4.1 定位：为什么会 lag？
- 消费逻辑慢：下游 DB/HTTP 慢、锁竞争、批处理不合理。
- 并行度不足：分区数太少、组内消费者数不足。
- rebalance 频繁：会话超时、处理时间过长导致 `max.poll.interval.ms` 触发。
- broker/网络问题：磁盘 I/O、ISR 频繁变化、带宽不足。

#### 4.2 止血：快速把 lag 拉下来
- **扩容消费者实例**（上限受分区数限制）。
- **增加分区**（需要评估 key 分布与顺序影响；分区变更会改变分配）。
- **提高单实例吞吐**：
	- 批量处理：拉大 `max.poll.records`/批写 DB
	- 并发处理：工作池并行（注意 offset 提交策略与顺序）
	- 调大 fetch（如 `fetch.min.bytes`/`fetch.max.wait.ms`）以提升吞吐
- **旁路削峰**：把重任务拆到内部队列/异步任务，消费者只做轻处理。

#### 4.3 治理：长期避免再次积压
- 关键链路做压测与容量评估（按峰值 TPS、消息大小、处理耗时算出所需分区与实例数）。
- 对“慢分支”做隔离：独立 topic/独立 consumer group。
- 失败重试与 DLQ：毒丸消息不要无限重试拖垮主消费。

**面试官：** 增加分区会影响什么？

**候选人：**
- **顺序**：只保证分区内顺序；增加分区可能导致同 key 落到不同分区（如果 key 选择不当）。
- **key 分布**：如果 key 倾斜，单分区热点仍会积压。
- **消费扩容上限**：分区数决定组内最大并行度。

---

### 5）可靠性与运维参数（加分项）

**面试官：** 怎么配置更可靠？

**候选人：** 常见组合思路：
- 生产者：`acks=all` + 开启幂等 + 合理重试与超时
- broker：`min.insync.replicas` 合理配置，避免单副本确认
- 避免不安全选主：`unclean.leader.election=false`（更偏可靠，代价是不可用时更容易停写）
- 主题层：设置合适的 `replication.factor`、保留策略（retention / compaction）

---

## RabbitMQ

### 1）RabbitMQ 核心模型（exchange/queue/binding）

**面试官：** RabbitMQ 的核心概念讲一下。

**候选人：**
- **Producer → Exchange → Queue → Consumer**
- **Exchange 类型**：
	- direct：按 routing key 精准路由
	- topic：通配符路由（`a.*`、`a.#`）
	- fanout：广播
	- headers：按 header 匹配
- **Binding**：队列与交换机的绑定规则。

**面试官：** ACK、prefetch 是什么？

**候选人：**
- **ACK**：消费者确认；不 ack 的消息会重新入队/重投。
- **prefetch（QoS）**：限制未确认消息数量，避免单个消费者拉太多导致内存堆积，也能实现更公平的分发。

---

### 2）如何保证不丢消息？（RabbitMQ）

**面试官：** RabbitMQ 如何做到消息尽量不丢？

**候选人：**
- **生产端确认**：publisher confirm（确保 broker 接收并落盘/复制）。
- **队列/消息持久化**：队列 durable、消息 persistent（仍要注意落盘时机与集群模式）。
- **消费端手动 ack**：业务成功后再 ack，失败走重试/DLQ。
- **高可用队列**：优先使用 quorum queue（基于 Raft）提升一致性与可恢复性。

---

### 3）RabbitMQ：重试、死信、延迟消息怎么做？

**面试官：** 失败重试怎么设计？

**候选人：**
- 不建议无限立即重试（会放大风暴）。
- 常见做法：
	- TTL + DLX：消息进入延迟队列，TTL 到期死信到重试队列。
	- 多级延迟：1m/5m/30m…
	- 最终进入 DLQ（死信队列）人工/离线处理。

---

### 4）Kafka vs RabbitMQ：怎么选？

**面试官：** 你怎么选择 Kafka 还是 RabbitMQ？

**候选人：**
- Kafka：更擅长高吞吐日志流、事件溯源、消费可回放、批处理/流处理生态。
- RabbitMQ：更擅长复杂路由、低延迟任务分发、请求-响应式的工作队列。

落地上还要看：顺序要求、重试语义、积压承载、运维成熟度、团队经验。
