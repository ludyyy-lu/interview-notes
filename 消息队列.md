# Kafka

## kafka的topic、partition、offset是什么？有什么用处？
## 生产者消费者的工作流程
## 怎么解决消息积压
转到消息中间件Kafka，其核心概念Topic是逻辑上的类别，Partition是物理上的分区，而Offset则是消息在分区中的唯一偏移量。分区设计带来了极大的扩展性，允许生产者并行写入和消费者并行消费。生产者将消息追加到分区日志末尾，消费者通过提交Offset记录消费进度。当遇到消息积压时，我们通常需要先定位是消费逻辑过慢还是分区数不足，解决方案通常包括临时增加消费者实例、提升消费端的并行度，或者将消息二次分发到内部队列进行异步处理。

# Rabbitmq