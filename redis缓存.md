# Redis 高频面试题 · 对话版

> 关键词：数据结构、缓存风险、持久化、复制/集群、性能与安全。

---

## 1）常用的 Redis 数据类型及使用场景

**面试官：** Redis 常用数据类型有哪些？各自适合什么场景？

**候选人：** 我会按“结构能力 + 常见命令 + 典型坑”来回答：

### String
- **能力**：二进制安全字符串；也能当计数器/简单对象载体。
- **常见场景**：
	- 缓存对象（序列化 JSON/Proto）
	- 计数器（`INCR/DECR`）
	- 分布式锁的 key（更推荐 Lua 原子脚本或 Redlock 讨论）
- **注意**：大 value 会带来网络与内存压力，尽量控制体积；频繁修改大 value 会增加复制/AOF 开销。

### Hash
- **能力**：字段-值，适合存对象的多个属性。
- **常见场景**：用户画像/会话信息、配置项。
- **注意**：字段数量巨大时注意 big key；字段访问虽然是 O(1) 期望，但整体内存布局与编码（listpack/hashtable）会影响内存。

### List
- **能力**：双端链表/列表；支持阻塞弹出。
- **常见场景**：简单队列（`LPUSH/RPOP`）、延迟不高的任务列表（`BRPOP/BLPOP`）。
- **注意**：List 做消息队列缺少“消费确认/重试/持久化语义”；更可靠的队列推荐 Streams 或专业 MQ。

### Set
- **能力**：无序去重集合，支持交并差。
- **常见场景**：去重（UV）、共同好友（交集）、标签系统。
- **注意**：集合过大时注意内存；交并差是集合运算，复杂度随集合规模增长。

### ZSet（Sorted Set）
- **能力**：有序集合（member 唯一，score 排序），支持 rank/range。
- **常见场景**：排行榜、延迟队列（按时间戳 score）、topN 统计。
- **注意**：排名是按 score 排序，tie-break 会按 member 字典序；需要业务定义“同分怎么排”。

### Bitmap / HyperLogLog / GEO / Stream（加分项）
- **Bitmap**：位图（`SETBIT/GETBIT/BITCOUNT`）适合签到、活跃天统计。
- **HyperLogLog**：基数统计（UV 近似），省内存但有误差。
- **GEO**：地理位置附近的人/店。
- **Stream**：更像轻量 MQ（消费组、ack、pending），适合“至少一次 + 可追溯”。

**面试官：** 你怎么避免“big key”问题？

**候选人：** 主要策略：
- 设计上拆分（按业务维度/时间分桶），避免一个 key 无限增长。
- 对 List/Hash/Set/ZSet 做容量治理：定期 `ZREMRANGEBYRANK`、`LTRIM`、过期策略。
- 运维上监控 bigkey（如定期抽样扫描），控制单 key 体积与成员数量。

---

## 2）ZSet 怎么设计排行榜？注意事项？

**面试官：** 用 ZSet 设计排行榜怎么做？

**候选人：** 典型建模：一个榜单一个 key。
- key：`rank:game:{season}` 或 `rank:activity:{yyyymmdd}`
- member：`user_id`
- score：`points`（积分）或复合 score（积分 + 时间戳 tie-break）

常见操作（以高分在前为例）：
```text
ZADD rank:game:202602 100 user:1
ZINCRBY rank:game:202602 5 user:1
ZREVRANGE rank:game:202602 0 9 WITHSCORES
ZREVRANK rank:game:202602 user:1
```

**面试官：** 复合 score 你怎么做？

**候选人：** 两种常见思路：
1. **score 存“主维度”**，同分的 tie-break 交给 member（比如 member 拼上反转时间戳）。缺点：member 变复杂。
2. **score 复合**：把积分做高位、时间做低位（或反之），比如 `score = points * 1e13 + (max_ts - ts)`，这样同分时按更早/更晚排序。

我更倾向方案 1 或者把“同分排序”在业务侧明确后再选：因为 score 是 double，过大的整数会有精度风险，需要控制量级。

**面试官：** 排行榜有哪些坑？

**候选人：**
- **容量治理**：榜单成员可能无限增长，常见做法是只保留 topK + 参与过的用户集合，或按周期滚动（天/周/月榜）。
- **热 key**：top10 查询非常频繁，需要缓存结果或做副本读；必要时把 top10 单独缓存到 String。
- **高频更新**：`ZINCRBY` 写放大 + 复制/AOF 成本；可以用本地累加后批量写入（但会牺牲实时性）。
- **一致性**：如果“积分”来自 DB，先写 DB 再更新榜单，或者采用异步事件更新（最终一致）。

---

## 3）只展示前十，其他用户怎么查自己排名？

**面试官：** 如果页面只展示 top10，那非 top10 用户如何查自己排名？

**候选人：** 用 `ZREVRANK`（或 `ZRANK`）查该 member 的 rank，再按 rank 拉取“附近窗口”。

典型流程：
1. `rank = ZREVRANK key user_id`（返回从 0 开始）
2. `score = ZSCORE key user_id`
3. 拉一个窗口：`ZREVRANGE key max(rank-3,0) rank+3 WITHSCORES`

**面试官：** 如果用户不在榜单里呢？

**候选人：** `ZREVRANK` 返回 nil，此时按业务：
- 未上榜：提示“暂无排名”，给出参与方式
- 或者把“参与过但未上榜”也写入 ZSet（score=0），但要注意容量。

---

## 4）缓存穿透、击穿、雪崩：是什么？怎么解决？

**面试官：** 解释下穿透、击穿、雪崩，并给出解决方案。

**候选人：** 我按“现象 → 根因 → 组合拳”回答。

### 缓存穿透（Cache Penetration）
- **现象**：大量请求查询“根本不存在”的数据，缓存永远 miss，打到 DB。
- **方案**：
	- **缓存空值**：对不存在的结果写入短 TTL 的空对象（注意与“真实空”区分）。
	- **布隆过滤器**：请求前先判断 key 是否可能存在；适合大规模 key 集。
	- **入参校验 + 限流**：明显非法的请求直接挡住。

### 缓存击穿（Cache Breakdown / Hot Key Expire）
- **现象**：某个热点 key 过期瞬间，大量并发同时回源 DB。
- **方案**：
	- **互斥重建**：用分布式锁/本地 singleflight 保证只有一个线程回源重建。
	- **逻辑过期**：value 里带过期时间，过期后先返回旧值，同时异步刷新。
	- **热点 key 永不过期 + 主动更新**：配合消息/定时任务刷新。

### 缓存雪崩（Cache Avalanche）
- **现象**：大量 key 同时过期或缓存服务整体不可用，导致 DB 被瞬间打爆。
- **方案**：
	- **TTL 随机化**：给过期时间加随机抖动，避免同一时刻失效。
	- **多级缓存**：本地缓存 + Redis；或者 Redis + CDN。
	- **降级/熔断/限流**：保护 DB 与核心链路。
	- **隔离**：热点业务与非热点业务分库分缓存集群。

**面试官：** 这些方案你线上怎么组合？

**候选人：** 常用组合：
- 先用 **TTL 抖动** 降低雪崩概率；
- 热点 key 用 **逻辑过期/永不过期 + 异步刷新**；
- 回源路径上加 **singleflight/分布式锁**；
- 对不存在的数据用 **空值缓存/布隆**。

---

## 5）怎么保证缓存与数据库一致性？

**面试官：** 缓存和数据库一致性怎么保证？

**候选人：** 先明确：缓存是为了性能，通常追求 **最终一致**，强一致需要付出很高成本。

### 最常用：Cache Aside（旁路缓存）
- **读**：先读缓存，miss → 查 DB → 写缓存（带 TTL）。
- **写**：先写 DB，再删缓存（而不是更新缓存）。

**面试官：** 为什么是“写 DB 后删缓存”？

**候选人：** 更新缓存容易出现并发覆盖与脏写；删缓存能迫使下次读走“回源重建”，避免把旧值长期留在缓存里。

**面试官：** 还是会有并发问题吧？举个例子。

**候选人：** 会有经典竞态：
1）线程 A 更新 DB 完成，准备删缓存；
2）线程 B 读缓存 miss，查到 DB 旧值/新值取决于时序，然后把值写入缓存；
3）A 删缓存（或没删到），导致短时间不一致。

缓解方式：
- **延迟双删**：写 DB 后立刻删一次缓存，隔一小段时间再删一次（覆盖“并发读回源”窗口）。
- **互斥重建**：读 miss 时用锁保证只有一个回源写缓存。
- **版本号/时间戳**：缓存 value 带版本，写入时做 compare，避免旧值覆盖新值。
- **订阅 binlog/CDC**：用消息总线驱动缓存失效（更稳，但系统复杂度更高）。

**面试官：** 你会用“先删缓存再写 DB”吗？

**候选人：** 一般不推荐。因为删缓存后到写 DB 成功前的窗口，读请求会回源拿到旧值并写回缓存，导致更长的不一致。

---

## 6）Redis 持久化方式有哪些？优缺点？

**面试官：** Redis 持久化有哪些？

**候选人：** 主要三类：

### RDB（快照）
- **机制**：周期性把内存数据生成快照文件（`dump.rdb`），常由 `BGSAVE` 触发。
- **优点**：文件紧凑、恢复快、适合备份/全量迁移。
- **缺点**：可能丢失最后一次快照后的数据；生成快照有额外 I/O 与 fork 成本。

### AOF（追加日志）
- **机制**：把写命令按顺序追加到 AOF；可配置刷盘策略。
- **刷盘策略**：
	- `always`：最安全，最慢
	- `everysec`：常用，最多丢 1 秒
	- `no`：依赖 OS 刷盘，风险大
- **优点**：数据更完整，可读性强（本质是命令日志）。
- **缺点**：文件大、恢复慢（需要重放）；需要 AOF rewrite 压缩。

### 混合持久化（RDB preamble + AOF）
- **机制**：AOF 重写时前半段是 RDB，后半段是增量 AOF。
- **优点**：兼顾恢复速度与数据完整性。

**面试官：** fork 成本你怎么解释？

**候选人：** RDB/AOF rewrite 都可能 fork 子进程。fork 本身快，但会触发 COW（写时复制），如果主进程在 fork 后仍大量写，会造成额外内存与页复制压力，导致延迟抖动。

---

## 7）Redis 主从复制怎么实现？注意事项？

**面试官：** Redis 的复制流程讲一下。

**候选人：** 核心是“全量同步 + 增量同步”：
- **全量**：从库发起同步，主库生成 RDB（或无盘复制），传给从库；从库加载快照。
- **增量**：之后主库把写命令以 replication stream 形式持续发送给从库。
- **部分重同步**：断线重连时尝试 `PSYNC`，利用 replication backlog 只补缺口。

**面试官：** 复制有什么坑？

**候选人：**
- **主从延迟**：读从库会读到旧值；写后读要“粘主”或加一致性策略。
- **全量同步开销大**：RDB 生成与传输会抖动；需要合理的带宽、磁盘与 `repl-backlog-size`。
- **故障切换**：需要 Sentinel/Cluster 或外部系统做自动 failover。

---

## 8）Redis Cluster 如何实现？优缺点？

**面试官：** Redis Cluster 的分片原理是什么？

**候选人：** Cluster 把 key 映射到 **16384 个 hash slot**，每个节点负责一部分 slot；客户端按 slot 路由。
- 跨节点访问会收到重定向：`MOVED`（稳定归属）或 `ASK`（迁移中的临时归属）。
- 支持在线迁移 slot（reshard）。

**面试官：** 有哪些限制？

**候选人：**
- **多 key 操作限制**：多 key 必须在同一 slot，否则像事务/Lua/mset 这类要么报错要么不可用。
- 解决方法：用 **hash tag**，例如 `{user:1}:profile`、`{user:1}:posts` 强制同 slot。

**面试官：** Cluster 优缺点？

**候选人：**
- **优点**：水平扩展、节点级故障转移、高吞吐。
- **缺点**：多 key/事务能力受限；运维复杂度更高；一致性仍是最终一致（主从复制延迟）。

---

## 9）Redis 内存管理机制？如何优化内存？

**面试官：** Redis 内存你怎么管控？

**候选人：** 重点是三块：
1. **容量上限**：`maxmemory` + 选择淘汰策略。
2. **淘汰策略**（常见）：
	 - `allkeys-lru` / `volatile-lru`
	 - `allkeys-lfu` / `volatile-lfu`
	 - `allkeys-random` / `volatile-random`
	 - `volatile-ttl`
3. **编码与碎片**：不同类型在元素较小时会用紧凑编码（如 listpack），超过阈值转成 hashtable/quicklist；另外 jemalloc 会有碎片，需要关注 `mem_fragmentation_ratio`。

**面试官：** 线上怎么优化？

**候选人：**
- 设计上：
	- 少用海量小 key（元数据开销大），可用 Hash 聚合。
	- 控制 big key；热数据分散。
- 配置上：
	- 设置合理 `maxmemory` 与淘汰策略（缓存场景一般 `allkeys-lfu/lru`）。
	- 过期时间加随机抖动，减少雪崩。
- 运维上：
	- 监控 `used_memory`、`evicted_keys`、`keyspace_hits/misses`、碎片率。
	- 避免在高峰做 `BGSAVE/AOF rewrite`。

---

## 10）Redis 事务机制怎么实现？限制是什么？

**面试官：** Redis 的事务是什么语义？

**候选人：** Redis 用 `MULTI/EXEC` 把命令打包顺序执行：
- 保证**队列里的命令按顺序执行**；
- 但不是数据库那种 ACID：
	- **没有回滚**（执行中某条命令失败，其他命令仍会执行）
	- 隔离性也有限（主要是单线程串行执行带来的“表面原子”，但与快照读无关）

并发控制主要靠 `WATCH` 做乐观锁：key 被其他客户端改动则 `EXEC` 失败，客户端重试。

---

## 11）Lua 脚本怎么工作？场景？

**面试官：** Lua 为什么常用于 Redis 原子操作？

**候选人：** 因为 Redis 单线程执行脚本，脚本执行期间不会被其他命令打断，天然具备“多命令原子性”。

**常见场景：**
- 分布式锁：校验 token 并删除（避免误删别人的锁）
- 限流：滑动窗口/令牌桶
- 库存扣减：校验 + 扣减 + 记录流水
- 幂等：用 setnx/哈希结构记录处理状态

**注意**：脚本不要太慢/循环太大，否则会阻塞 Redis；必要时用超时保护与拆分。

---

## 12）发布/订阅（Pub/Sub）怎么实现？场景？

**面试官：** Pub/Sub 能当消息队列吗？

**候选人：** 不建议。Pub/Sub 是**实时广播**，不持久化、不支持消费确认；消费者离线会丢消息。

**适用场景：** 在线通知、聊天室广播、配置变更通知。
需要可靠消费请用 Streams 或专业 MQ。

---

## 13）管道（Pipeline）是什么？场景？

**面试官：** Pipeline 的收益来自哪里？

**候选人：** 主要来自减少网络 RTT：客户端一次发送多条命令，Redis 批量返回结果。

**注意**：Pipeline 不是事务，不保证原子；批次过大可能导致单次响应过大、阻塞与内存峰值。

---

## 14）慢查询日志（SLOWLOG）怎么用？怎么优化慢查询？

**面试官：** Redis 慢查询怎么排查？

**候选人：**
- 开启并配置：
	- `slowlog-log-slower-than`（微秒阈值）
	- `slowlog-max-len`（保留条数）
- 常用命令：`SLOWLOG GET`、`SLOWLOG LEN`。

优化方向：
- 避免 O(n) 重操作（如对超大集合做全量遍历）。
- 拆 big key；用 scan 系列（`SCAN/HSCAN/SSCAN/ZSCAN`）渐进遍历。
- 降低单次返回数据量（分页、topN）。

---

## 15）安全机制有哪些？怎么保护 Redis？

**面试官：** Redis 安全怎么做？

**候选人：**
- 网络层：内网隔离、安全组/防火墙、限制来源；不要暴露公网。
- 配置层：
	- `protected-mode yes`
	- `bind 127.0.0.1` 或绑定内网地址
	- ACL（`ACL SETUSER`）最小权限
	- 禁用危险命令（如 `FLUSHALL/CONFIG/KEYS`，按需 `rename-command`）
- 传输层：支持的话启用 TLS。
- 运维层：审计、备份、告警；防止弱口令与未授权访问。

---

## 16）监控与性能调优：常用工具和方法

**面试官：** 你线上会看哪些指标？

**候选人：**
- 命中率：`keyspace_hits/misses`
- 内存：`used_memory`、`mem_fragmentation_ratio`、`evicted_keys`
- 延迟：`latency` 事件、`instantaneous_ops_per_sec`
- 客户端：连接数、阻塞客户端数
- 持久化：RDB/AOF 状态、rewrite 耗时
- 复制：`master_repl_offset`、`slave_repl_offset`、lag

工具：
- `redis-cli info`、`redis-cli --latency`、`redis-cli --stat`
- Prometheus Exporter + Grafana
- `redis-benchmark`（压测要贴近业务模型）

---

## 17）备份与恢复怎么做？注意事项？

**面试官：** Redis 怎么备份？恢复时注意什么？

**候选人：**
- **备份**：
	- 基于 RDB：触发 `BGSAVE` 后复制 `dump.rdb` 到安全存储（最好在从库上做，减少主库抖动）。
	- 基于 AOF：备份 AOF 文件（注意 rewrite 过程中切换文件）。
- **恢复**：
	- 关闭实例 → 放置 RDB/AOF 到数据目录 → 启动加载。
- **注意**：
	- 备份要做校验与演练（能否恢复比“有备份”更重要）。
	- 大实例恢复时间长，需要评估 RTO；必要时做分片与分级备份。
