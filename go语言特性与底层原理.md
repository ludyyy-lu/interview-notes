# channel
# go map 的底层实现
# sync.Map 的底层实现
# GMP模型

Channel是实现CSP模型的核心，它通过hchan结构体维护了一个环形缓冲区和等待队列，利用锁机制保证了并发安全。Map的底层实现则是基于哈希表，采用桶（bucket）结构存储键值对，并在装载因子过高时通过渐进式搬迁进行扩容，以避免瞬时的性能抖动。sync.Map则是专门为读多写少场景优化的并发安全Map，它通过read和dirty两个原生map的读写分离，利用原子操作减少了锁的竞争。而GMP模型是Go调度器的精髓，G代表协程，M代表内核线程，P代表处理器资源。通过Work Stealing（任务窃取）和Hand Off（转交P）机制，Go能在用户态实现高效的并发调度，极大降低了线程切换的开销。