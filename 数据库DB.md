# MySQL的索引你了解的有哪些？
在MySQL的索引体系中，我们最常用的主要是B+树索引、Hash索引以及全文索引。索引之所以能让查询变快，其核心思想是空间换时间，将无序的数据通过特定的数据结构转化为有序的组织形式，从而将全表扫描的线性查找复杂度降低到对数级别。B+树作为InnoDB存储引擎的首选，其优势在于它只有叶子节点存储实际数据，非叶子节点仅存储键值和指针，这使得每个节点能容纳更多的索引项，极大地增加了分支因子，降低了树的高度。在磁盘IO场景下，这意味着只需极少的磁盘预读即可定位数据。此外，B+树的叶子节点通过双向链表相连，这完美支持了范围查询。所谓的“回表”现象，通常发生在二级索引查询中，因为二级索引的叶子节点只存储了主键值，如果查询需要的字段不在该索引中，引擎就需要拿着主键回到聚簇索引中再次查找完整的行记录。
# 为什么使用索引就会变快？索引实现？B+树？
# 慢查询怎么应对？
# 怎么分表？
# 事务的四大特性？
面对慢查询，我们首先会利用EXPLAIN工具分析执行计划，观察是否触发了全表扫描、索引失效或者是扫描行数过多。应对策略通常包括优化索引结构、避免在索引列上进行函数计算、或者通过覆盖索引减少回表。当单表数据量突破千万级甚至更大导致B+树层级增高或写入性能下降时，就需要考虑分表。垂直分表是将大字段或不常用的字段拆分到新表，减少单行长度；水平分表则是通过Hash或Range将数据分散到多个结构相同的表中。在事务层面，ACID四大特性是基石，其中原子性保证操作要么全成功要么全撤回，一致性确保状态转换合法，隔离性处理并发冲突，持久性则保障数据落地。隔离级别的不同直接引发了脏读、不可重复读和幻读问题，MySQL通过MVCC多版本并发控制和Next-Key Lock解决了这些问题，默认的Repeatable Read级别下，通过间隙锁很大程度上避免了幻读。
# 什么是锁？MySQL的锁有哪些？行锁和表锁的区别？死锁？
锁机制是数据库并发控制的核心。MySQL中的锁可以分为表锁和行锁，行锁粒度小、并发度高但开销大且可能死锁，表锁则相反。当两个事务互相等待对方持有的资源时就会发生死锁，InnoDB通过死锁检测机制主动回滚权重较小的事务来解除僵局。在数据库设计上，范式是减少数据冗余的准则，第一范式要求原子性，第二范式要求消除部分依赖，第三范式要求消除传递依赖，实际开发中我们往往为了性能而进行适当的反范式设计。
# 什么是范式？常见的范式有哪些？
# 隔离级别、问题与解决方案
# 什么是回表？
--- 

# 什么是主从复制？主从复制的原理？如何实现主从复制？
# 什么是分布式数据库？常见的分布式数据库有哪些？分布式数据库的优缺点？
